# 服务配置
server:
  port: 8082
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/junze?serverTimezone=UTC
    username: root
    password: abc123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml  #sql映射文件位置
  configuration:
    map-underscore-to-camel-case: true